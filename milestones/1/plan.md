# 🟩 Milestone 1 — TCP Server Core

**Goal:** accept a single client and reply.

## Tasks

- Create socket
- Bind to port
- Listen
- Accept connection
- Read bytes
- Write bytes
- Close connection

## Output

- `curl http://localhost:8080` works
- Always returns `"Hello World"`

